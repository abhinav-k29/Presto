describe('Admin Happy Path', () => {
  it('successfully handles the happy path for an admin', () => {
    // cy.visit('http://localhost:3000/register');
    // cy.get('input[name="email"]').type('admin');
    // cy.get('input[name="password"]').type('admin123{enter}');

    // cy.url().should('include', 'http://localhost:3000/dashboard');
    // cy.get('[data-cy=create-presentation]').click();
    // cy.get('input[name="presentation-name"]').type('New Presentation{enter}');

    // cy.contains('Presentation created successfully').should('exist');

    // cy.contains('Thumbnail updated successfully').should('exist');
    // cy.get('[data-cy=add-slide]').click();
    // cy.get('[data-cy=slide-title]').type('Slide 1{enter}');

    // cy.contains('Slide added successfully').should('exist');
    // cy.get('[data-cy=logout]').click();

    // cy.url().should('include', 'http://localhost:3000/login');
    // cy.get('input[name="username"]').type('admin');
    // cy.get('input[name="password"]').type('admin123{enter}');

    // cy.url().should('include', 'http://localhost:3000/dashboard');
    // cy.contains('Welcome back, admin').should('exist');
  });
});
